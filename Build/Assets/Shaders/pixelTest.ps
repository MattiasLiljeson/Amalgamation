cbuffer PixelProgramCBuffer
{
	float4 color;
};
/*
Texture2D Texture;
SamplerState SampleType
{
	Filter = MIN_MAG_MIP_POINT;
    AddressU = Wrap;
    AddressV = Wrap;
};
*/

struct VertexOut
{
    float4 position	: SV_POSITION;
	float2 texCoord	: TEXCOORD;
};

float4 PS(VertexOut p_input) : SV_TARGET
{
	return color;
	//return float4(p_input.texCoord.x,p_input.texCoord.y,1.0f,1.0f);
}

